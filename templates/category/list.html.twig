{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1>Blabla </h1>
    {% for category in categories %}
    <div><a href="{{path('app_category', { id : category.id })}}"> {{ category.title }} </a></div>
    {% endfor %}

{% endblock %}
